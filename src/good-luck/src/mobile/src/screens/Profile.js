import * as React from 'react';
import {
  TouchableOpacity,
  StyleSheet,
  View,
  Text,
  SafeAreaView,
} from 'react-native';

const Profile = ({ navigation }) => {
  return <div></div>;
};
export default Profile;
