import * as React from 'react';
import {
  TouchableOpacity,
  StyleSheet,
  View,
  Text,
  SafeAreaView,
} from 'react-native';

const Profile = ({ navigation }) => {
  return <View></View>;
};
export default Profile;
