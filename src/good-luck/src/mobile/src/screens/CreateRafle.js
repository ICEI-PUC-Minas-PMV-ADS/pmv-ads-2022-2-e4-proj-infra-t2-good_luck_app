import * as React from 'react';
import {
  TouchableOpacity,
  StyleSheet,
  View,
  Text,
  SafeAreaView,
} from 'react-native';

const CreateRafle = ({ navigation }) => {
    return(
        <div>

        </div>
    )
}
export default CreateRafle;